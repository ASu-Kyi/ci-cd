name: Build workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test
        run: |
          Write-Output "Amigoscodes"
          "Hello" | Out-File -FilePath file.txt
          Get-ChildItem
          Get-Content file.txt
