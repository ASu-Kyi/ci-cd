name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  myFirstJob:
    runs-on: windows-latest

    steps:
      - name: My First Action
        uses: actions/checkout@v6
      - name: My First Step
        run: |
          Get-ChildItem Env:
